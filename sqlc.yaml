version: "2"
sql:
  - engine: "postgresql"
    schema: ".database/migrations"
    queries: ".database/queries"
    gen:
      go:
        package: "db"
        out: "db"
        sql_package: "pgx/v5"
        emit_empty_slices: true
